![Project Overview](images/overview.png)

When using g++, compile the project using
g++ main.cpp lib/vectors.cpp lib/writers.cpp lib/kutta.cpp -Ilib -o main
we assume the structure of the repository mirrors the one on the user's machine.
